import turtle

# Create one turtle for drawing.
t = turtle.Turtle()
t.shape("turtle")

# Move and turn a few times.
t.forward(100)
t.left(90)
t.forward(50)
t.right(90)
t.forward(100)

# Keep the turtle window open.
turtle.mainloop()
